import { IAddButtonStyleProps, IAddButtonStyles } from "./addButton.types";

export function AddButtonStyles(props: IAddButtonStyleProps): IAddButtonStyles {
    return {
        root: {
            fontSize: '20px',
        },
    }
}