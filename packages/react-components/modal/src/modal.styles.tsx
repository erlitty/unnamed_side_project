import { IModalStyleProps, IModalStyles } from "./modal.types";

export function ModalStyles(props: IModalStyleProps): IModalStyles {
    return {
        root: {
            fontSize: '20px',
        },
    }
}